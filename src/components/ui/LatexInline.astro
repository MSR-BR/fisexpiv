---
import katex from "katex";

interface Props {
  expression: string;
}

const { expression } = Astro.props;

const html = katex.renderToString(expression, {
  displayMode: false,
  throwOnError: false,
  strict: "ignore",
});
---

<span class="latex-inline" set:html={html} />
