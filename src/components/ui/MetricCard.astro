---
interface Props {
  value: string | number;
  label: string;
}

const { value, label } = Astro.props;
---

<article class="metric-card">
  <strong>{value}</strong>
  <span>{label}</span>
</article>

